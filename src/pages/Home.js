import React from "react";
import { Cards, Container, Header } from "../components";

const Home = () => {
  return (
    <Container>
      <Header />
      <Cards />
    </Container>
  );
};

export default Home;
